export const doLogin = async (e) => {
  e.preventDefault();
  console.log("Login with Google");
};

export const doLoginGoogle = async (e) => {
  e.preventDefault();
  console.log("Login with Google");
};
